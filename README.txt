
    $ usb_device_info.py
    ~~~~~~~~^
   |
    ~~~~~~~< a simple usb info-getter

 dependencies:
 `pip install click`, and then `pip install pyusb --pre`

 usage:
 $ usb_device_info.py <vid> <pid>

 output is human-readable.

 